[Unit]
Description=gunicorn daemon for MyFunction
After=network.target

[Service]
User=root
Group=root
WorkingDirectory=/srv/MyFunction
ExecStart=/srv/MyFunction/venv/bin/gunicorn --workers 3 --bind 127.0.0.1:8000 MyFunction.wsgi:application

[Install]
WantedBy=multi-user.target
